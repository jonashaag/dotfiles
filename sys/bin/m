#!/usr/bin/env python3
import sys
import operator
import functools

op = sys.argv[1]
op = {
  '*': 'mul',
  '+': 'add',
  '/': 'div',
  '-': 'sub',
}.get(op, op)
op = getattr(operator, op)

stdin = [float(x) for x in sys.stdin.read().split()]
print(functools.reduce(op, stdin))
