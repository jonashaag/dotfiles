#!/bin/sh

~/sys/bin/mount-exthdd "$1"
if [ 0 -ne $? ]
then
    exit $?
fi

echo 'bereeiiit?'
read
echo 'go go go'
rsync -arv --exclude-from=$HOME/.rsync-exclude ~ /media/exthdd/

~/sys/bin/mount-exthdd "$1"
