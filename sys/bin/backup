#!/bin/sh

safe_exec() {
    $@
    if [ 0 -ne $? ]
    then
        exit $?
    fi
}

safe_exec ~/sys/bin/mount-exthdd "$1"
safe_exec rsync -arv --exclude-from=$HOME/.rsync-exclude ~ /media/exthdd/
safe_exec ~/sys/bin/mount-exthdd "$1"
