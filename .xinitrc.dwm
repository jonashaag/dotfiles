#!/bin/sh

# D-Bus
eval "$(dbus-launch --sh-syntax --exit-with-session)"

xbindkeys &

# Keyring
eval $(gnome-keyring-daemon)
export GNOME_KEYRING_PID GNOME_KEYRING_SOCKET SSH_AUTH_SOCK

.bin/dwm &
.bin/dwm-statusbar.py 1 &

# Dummy IDLE process
exec .bin/x-session
